[project]
name = "resource-catalog"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pytest",
    "httpx"
]

[tool.pytest.ini_options]
pythonpath = ["."]
